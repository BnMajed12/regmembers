<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.regdemo"
    android:versionCode="1"
    android:versionName="1.0" >
<uses-permission android:name="android.permission.INTERNET"/>
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
 <uses-feature android:name="android.hardware.camera" />   
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />
    <uses-feature />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.regdemo.SplashScreen"
             android:theme="@android:style/Theme.NoTitleBar"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login"
             android:theme="@android:style/Theme.Wallpaper.NoTitleBar"
             android:launchMode="standard"/>
         <activity android:name=".RegisterPageHolder"
              android:theme="@android:style/Theme.NoTitleBar"
             android:launchMode="standard"/>
        <activity android:name=".HomePage"
              android:theme="@android:style/Theme.NoTitleBar"
             android:launchMode="standard"/>
          <activity android:name=".MainActivity"
              android:theme="@android:style/Theme.NoTitleBar"
             android:launchMode="standard"/>
          <activity android:name=".FomuToSend"
              android:theme="@android:style/Theme.NoTitleBar"
             android:launchMode="standard"/>
          <activity android:name=".Hariri"
              android:theme="@android:style/Theme.NoTitleBar"
             android:launchMode="standard"/>
         <activity android:name=".NextPage"
             android:theme="@android:style/Theme.NoTitleBar"
             android:launchMode="standard"/>
          <activity android:name=".Biashara"
              android:theme="@android:style/Theme.NoTitleBar"
             android:launchMode="standard"/>
          <activity android:name=".Mdhamini"
              android:theme="@android:style/Theme.NoTitleBar"
             android:launchMode="standard"/>
           <activity android:name=".MtejaPicha"
             android:theme="@android:style/Theme.NoTitleBar"
             android:launchMode="standard"/>
    </application>

</manifest>
